import React from "react";
import ReactDOM from "react-dom/client";
import PandavkadsHeroPreview from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PandavkadsHeroPreview />
  </React.StrictMode>
);
